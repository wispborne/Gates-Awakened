version: 2.1
jobs:
    build:
        docker: 
            - image: alpine:latest
        steps:
            - run:
                  name: Zipping mod...
                  command: .circleci/zipMod.sh
                  shell: /bin/bash
